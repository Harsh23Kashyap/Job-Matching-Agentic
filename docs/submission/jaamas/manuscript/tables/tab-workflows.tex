\caption{Primary user-facing workflows by role ($K{=}10$ composite matching unless noted).}
\label{tab:workflows}
\scriptsize
\begin{tabular}{@{}p{0.13\linewidth}p{0.20\linewidth}p{0.20\linewidth}p{0.35\linewidth}@{}}
\toprule
Role & Portal route & User goal & API / agent path \\
\midrule
Candidate & \path{/candidate/onboarding} & Upload resume, review extraction, create profile & \path{POST /candidates/upload-resume} $\rightarrow$ \path{PUT /candidates/me} $\rightarrow$ Candidate agent registers snapshot + embedding \\
Candidate & \path{/candidate/profile} & Edit skills, experience, pay, remote prefs & \path{GET/PUT /candidates/me}; quality via \path{POST /candidates/quality-check} \\
Candidate & \path{/candidate/matches} & View ranked jobs, filter, explain scores & \path{POST /match/candidate-to-jobs} (composite); profile readiness gate before search \\
Candidate & \path{/candidate/saved} & Saved jobs and applications & \path{GET/PUT /candidates/me/saved-jobs}; \path{POST /candidates/me/applications} \\
Employer & \path{/employer/jobs} & Import JD, post/edit/close roles & \path{POST /jobs/upload-description} or \path{/parse-description} $\rightarrow$ \path{POST /jobs} or \path{PUT /jobs/mine/{id}} $\rightarrow$ Employer agent \\
Employer & \path{/employer/matches} & Rank candidates for an owned job & \path{POST /match/job-to-candidates} (composite); job ownership required \\
Employer & \path{/employer/applications} & Review applicants per posting & \path{GET /jobs/mine/applications} (SQLite activity store) \\
Admin & \path{/admin/console} & Monitor agents, run matches, inspect fairness & \path{GET /agents/status}, \path{/agents/events/recent}, \path{/system/config}; optional \path{/match/*}, \path{/system/fairness}, \path{POST /real-jobs/sync} when live feed enabled \\
\bottomrule
\end{tabular}
\normalsize
\par\vspace{0.4em}
\footnotesize{All authenticated routes use cookie sessions (\texttt{withCredentials}) and FastAPI \texttt{require\_role} guards. Match endpoints invoke the Matchmaking agent read-only over snapshots.}
